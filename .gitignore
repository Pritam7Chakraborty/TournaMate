# Dependencies
node_modules/
package-lock.json # Optional: some prefer to keep this, but it can cause conflicts in monorepos sometimes. Usually safe to keep.

# Environment variables
.env
.env.local
.env.production

# Build outputs
dist/
build/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS specific
.DS_Store
Thumbs.db